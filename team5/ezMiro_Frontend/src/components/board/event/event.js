const canvasToolMode = {
  mouseMode: 'MOUSE_MODE',
  postNoteMode: 'POST_NOTE_MODE',
  drawLineMode: 'DRAW_LINE_MODE'
}

const canvasEvent = {
  mouseDown: 'MOUSE_DOWN'
}

export { canvasToolMode, canvasEvent }
export default { canvasToolMode, canvasEvent }
